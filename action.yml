name: 'Hello World by yui666a'
author: 'yui666a'
description: 'Greet someone and record the time'
inputs:
  who-to-greet:  # id of input
    description: 'Who to greet'
    required: true
    default: 'World'
  branch-name:
    description: 'branch name should be "{prefix}/{issueNumber}/{issueTitle}"'
    required: true
# outputs:
#   time: # id of output
#     description: 'The time we greeted you'
runs:
  using: 'node16'
  main: 'index.js'

branding:
  icon: 'link'
  color: 'green'